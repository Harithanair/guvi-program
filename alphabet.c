#include<stdio.h>
int main()
{
char x;
printf("enter the alphabet");
scanf("%c",&x);
if(c>='a' && c<='z' || c>='A' && c<='Z')
printf("%c is alphabet",x);
else
printf("%c is not a alphabet",x);
return 0;
}
